import 'package:flutter/material.dart';

class SeanceScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Séances'),
      ),
      body: Center(
        child: Text('Séance Details'),
      ),
    );
  }
}
